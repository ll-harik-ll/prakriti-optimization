1.  Need to change useLocation hook to useContext hook or something similar. 
    useLocation does not persist data => lost on page reload
    --The above is for when we send report/response from QuizPage to ConfirmPage
